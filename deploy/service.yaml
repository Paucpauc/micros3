apiVersion: v1
kind: Service
metadata:
  name: micros3
spec:
  clusterIP: None
  selector:
    app: micros3
  ports:
    - name: s3
      port: 30010
      targetPort: 30010
    - name: internal
      port: 9001
      targetPort: 9001
